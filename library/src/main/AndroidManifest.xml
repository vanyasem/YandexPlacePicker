<?xml version="1.0" encoding="utf-8"?>
<manifest package="ru.semkin.yandexplacepicker"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application>

        <activity
            android:name=".ui.YandexPickerActivity"
            android:launchMode="singleTop"
            android:theme="@style/PickerTheme.NoStatus" />

        <activity android:name=".ui.SearchActivity"
            android:theme="@style/PickerTheme"
            android:parentActivityName=".ui.YandexPickerActivity" />

    </application>

</manifest>
